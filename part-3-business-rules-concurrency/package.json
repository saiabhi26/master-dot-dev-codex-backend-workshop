{
  "name": "auction-house",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "db:up": "../scripts/workshop-db up auction_part_3",
    "db:down": "../scripts/workshop-db drop auction_part_3",
    "db:reset": "npm run db:down && npm run db:up && npm run migrate",
    "migrate": "DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_3 npm run migrate --workspace server",
    "dev": "API_PORT=3103 WEB_PORT=5103 DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_3 concurrently -k -n server,web \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "start": "npm run db:up && npm run migrate && npm run dev",
    "test": "DATABASE_URL=postgres://auction:auction@localhost:55432/auction_part_3 npm run test --workspace server && npm run test --workspace web",
    "test:e2e:bidding-war": "playwright test e2e/bidding-war.spec.ts --headed --project=chrome",
    "test:e2e:bidding-rules": "playwright test e2e/bidding-rules.spec.ts --headed --project=chrome",
    "demo:bidding-war": "KEEP_BIDDING_WAR_OPEN=1 npm run test:e2e:bidding-war"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.2.1"
  }
}
