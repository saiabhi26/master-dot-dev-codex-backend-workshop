# Auction House — Part 3

```sh
npm install
npm start
```

`npm start` ensures the shared PostgreSQL container and `auction_part_3` database are ready,
runs pending migrations, and starts both apps. Open <http://localhost:5103> (API `3103`).
