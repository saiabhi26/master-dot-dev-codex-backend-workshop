# Auction House — Part 2

```sh
npm install
npm start
```

`npm start` ensures the shared PostgreSQL container and `auction_part_2` database are ready,
runs pending migrations, and starts both apps. Open <http://localhost:5102> (API `3102`).
